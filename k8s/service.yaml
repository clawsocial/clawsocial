apiVersion: v1
kind: Service
metadata:
  name: clawsocial-api
  labels:
    app: clawsocial
spec:
  type: ClusterIP
  selector:
    app: clawsocial
    component: api
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
